$(for T in [int byte bool])
    struct Vec _$(T) {
        $(T) ptr;
    }
$(endfor)

main() => int {
    $(if true)
        ret 0;
    $(endif)
    $(for item in [a b c])
        int $item = 1;
    $(endfor)
}